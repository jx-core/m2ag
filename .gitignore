annexes/

# Maven build output
target/

# M2AG : ATL output (reproducible via `mvn exec:java`)
models/*-deployment.xmi

# M2AG : M0 generated artifacts (reproducible via `mvn exec:java`)
generated/

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
bin/
.metadata/

# Visualization (Vite) build/install output
visualization/node_modules/
visualization/dist/

# Theia/VS Code extension build/install output
theia-extension/node_modules/
theia-extension/out/
*.vsix

# Claude Code session-local backups (the hook resolves cwd-relative, so match
# at any depth)
**/.claude/.edit-baks/
